const userSelectors = require('./userSelectors')
const productSelectors = require('./productSelectors')
const remoteUserSelectors = require('./remoteUserSelectors')

module.exports = {
  userSelectors,
  productSelectors,
  remoteUserSelectors
}
